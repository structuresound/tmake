apiVersion: v1
kind: Service
metadata:
  name: ingress-controller
  labels:
    app: ingress-controller
spec:
  type: LoadBalancer
  selector:
    app: ingress-controller
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https